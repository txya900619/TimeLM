defaults:
  - net: rnnlm.yaml
  - scheduler: reduce_lr_on_plateau.yaml
  - _self_

_target_: src.models.ldc_module.LDCLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0
